# Implemented Controls

This directory contains only controls with current operating mechanisms and verification evidence satisfying `deliberations/README.md`.

This directory is initially empty of control records.

Implementation may be invalidated. Preserve evidence and demote the current record to `controls/pending/` when its operating claim no longer holds.
